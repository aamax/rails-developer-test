<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
              data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=<%= root_path %>>Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <% if logged_in?(:user) %>
          <li>
            <a href="/articles">Article Index <span class="sr-only">(current)</span></a>
          </li>
        <% end %>

        <% if logged_in?(:admin) %>
            <li>
              <%= link_to "User Manager", users_path %>
            </li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">

        <% if user_signed_in? %>
            <div class="nav-item">
              <%= link_to current_user.email, user_path(current_user) %>
              &nbsp;&nbsp;
            </div>

            <%= link_to "Logout", destroy_user_session_path, method: :delete,
                        class: "btn btn-sm btn-info " %>
        <% else %>
          <% if request.env['PATH_INFO'] != '/users/sign_in' %>
            <%= link_to "Login / SignUp", new_user_session_path,
                        class: "btn btn-sm btn-primary login_btn" %>
          <% end %>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>